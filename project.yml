name: DecisionWheel
options:
  bundleIdPrefix: com.elevator-robot
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: "6.0"
targets:
  DecisionWheel:
    type: application
    platform: iOS
    sources:
      - DecisionWheel
    settings:
      base:
        INFOPLIST_FILE: DecisionWheel/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.elevator-robot.DecisionWheel
        DEVELOPMENT_TEAM: ""
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0
